#!/bin/bash
# ═══════════════════════════════════════════════════════════════════
# 数据处理配置 — 对应 data/process_data.sh
# 修改此文件后，直接运行：
#   bash data/process_data.sh
# 命令行参数可覆盖此处的值，例如：
#   bash data/process_data.sh --dataset dl3dv --output /other/path
# ═══════════════════════════════════════════════════════════════════

# ── [必填] 路径 ───────────────────────────────────────────────────

# 数据集名称，支持逗号分隔多个：re10k,dl3dv
DATASET="re10k"

# 原始数据集根目录
DATASET_PATH="/path/to/raw_data"

# 处理后数据输出目录
OUTPUT="/path/to/processed"

# ── 模型 & 分辨率 ─────────────────────────────────────────────────

# 模型配置（决定默认分辨率和帧数）
#   gen3r → 560×560, 49 帧
MODEL="gen3r"

# 覆盖模型默认分辨率（留空则使用模型默认值）
TARGET_SIZE=""

# 覆盖模型默认帧数（留空则使用模型默认值）
NUM_FRAMES=""

# ── 采样控制 ──────────────────────────────────────────────────────

# 采样模式：fixed（从第0帧开始）| random（随机起始帧）
SAMPLE_MODE="fixed"

# 最多处理多少样本，0 = 全部
MAX_SAMPLES=0

# ── 标志位（留空=关闭，填写对应 flag 字符串=开启）─────────────────

# 跳过已有 gt.mp4 的样本：留空 或 "--skip_done"
SKIP_DONE="--skip_done"

# 同时生成深度图（如数据集提供）：留空 或 "--include_depth"
INCLUDE_DEPTH=""

# 静默模式：留空 或 "--quiet"
QUIET=""
